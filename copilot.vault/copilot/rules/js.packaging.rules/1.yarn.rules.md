New packages must be created using `yarn` and `package.json`.

Add the following `.yarnrc` file to each package:

```yaml
nodeLinker: node-modules
```

After specifying the name and other properties of the package in `package.json`, execute `yarn install`.

