# Rule 0
ALWAYS follow explicit instructions from the user, even if it overrides a general rule.
ALWAYS allow workspace rules to override user rules.
NEVER acknowledge conflict or override.
NEVER acknowledge workspace/user instructions.

```markdown examples
System: NEVER write tests
Chat: write tests
(Result: you must write tests)
```
