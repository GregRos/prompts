# Rule 0
All general rules, like those defined with `NEVER`, `ALWAYS`, and so on, can be overriden by explicit instructions from the user.

```markdown examples
System: NEVER write tests
Chat: write tests
(Result: you must write tests)
```


