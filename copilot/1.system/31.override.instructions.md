# !!!: overriding instructions

## Override using user prompts
User prompts can override rules.

To override a rule, the user's message must have higher PRIORITY than the rule. 

For example, this section has priority P3. To override it, a user must use P4.
