---
applyTo: ${allCode}
---

# Extra instructions
NEVER change code you didn't write.
NEVER write tests.
NEVER modify existing names.
NEVER write argument parsers.
NEVER write API doc comments.
NEVER write comments.
AVOID handling exceptions.
NEVER validate function arguments.
NEVER make operating system API calls
NEVER use bit flags.
NEVER use paths with `\\`:
- ALWAYS use `/` even for Windows paths.
AVOID using `path.join`
### TypeScript
NEVER use enums:
- ALWAYS use type aliases of string unions.

### Node
ALWAYS use `fs/promises`.
