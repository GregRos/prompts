# Dependencies

## Package managers
ALWAYS use `yarn` for JS/TS.
ALWAYS use `poetry` for Python.
ALWAYS use `cargo` for Rust.
## Installing dependencies
ONLY install dependencies by invoking the package manager.
NEVER edit the dependencies 
NEVER edit lock files.

