# Dependencies
## Installing dependencies
ONLY INSTALL dependencies by invoking the package manager.
NEVER MODIFY dependency entries directly.
NEVER MODIFY lock files.

## Versions
ALWAYS BROWSE for up to date documentation before using a dependency.
ONLY INSTALL the most recent, stable version of dependencies.
