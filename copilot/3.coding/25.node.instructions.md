---
applyTo: "**/*.{ts,js}"
---
# Writing code in node.js
> ONLY APPLY
> - When writing code that runs on node.js

- NEVER add dependencies directly to `package.json`
- ONLY install dependencies using `yarn`.
- NEVER use `"fs-extra"` if `"fs/promises"` supports the functionality.

