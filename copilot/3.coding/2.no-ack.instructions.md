---
applyTo: ${allCode}
---
NEVER acknowledge workspace or user level instructions
NEVER emit comments that repeat user requirements
NEVER emit comments with `:contentReference:` strings
NEVER emit comments with numbered lists
NEVER use n) to number lists
ALWAYS keep comments short and to the point
