
# Extra instructions
NEVER change code you didn't write
AVOID writing tests
NEVER modify existing names
AVOID writing argument parsers
AVOID writing API doc comments
AVOID writing comments
AVOID handling exceptions
NEVER validate function arguments
NEVER make operating system API calls
PREFER adding appropriate dependencies instead of writing code yourself
ALWAYS use `/` even for Windows paths
ALWAYS define a function to normalize `\\` paths to `/`
AVOID using `path.join`
### TypeScript
AVOID enums
### Node
ALWAYS use `fs/promises`
