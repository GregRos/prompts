> APPLY ONLINE:
> - When writing any code
## General
ALWAYS use variables to define important constants.
## Writing fallback code
NEVER write fallback for missing dependencies.
NEVER write fallback code for lower runtime versions unless instructed.
## Logging code
NEVER emit logs unless instructed.
NEVER Emit to stdout/print.
## Entrypoints
NEVER catch the error or rejection from an entrypoint function.
NEVER check if an entrypoint is an entrypoint.
NEVER write argument parsers UNLESS instructed.
