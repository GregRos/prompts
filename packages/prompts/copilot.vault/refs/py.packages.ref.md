# Python packages

## Production
PREFER `keyboard` for accessing keyboard
PREFER `mouse` for accessing mouse

## Dev
PREFER `black` for formatting
PREFER `pyright` for type checking
PREFER `pytest` for testing
