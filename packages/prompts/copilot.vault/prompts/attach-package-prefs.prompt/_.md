---
name: attach-preferred-packages
description: Attaches links to preferred package lists
argument-hint: "{ language = ts }"
---
> REQUIRED: #{input:language}.

Refer to the org-wide preferred package lists and see if a package is listed for your use-case. Read only the package list that corresponds to #{input:language}.

MANDATORY: If a package is listed, use the package. Don't write your own code. Don't install other packages.
# if #{input:language} ≈ TypeScript
Read [[js.packages.ref|ref packages js]]
# if #{input:language} ≈ Python
Read [[py.packages.ref|ref packages py]]

![[prompts/error.prompt/_|_]]