If an input does not match the expected format, or a required input is not specified, emit a descriptive error response:

```
When running #{prompt file or command}, input #{input name} #{expectation}, but it was #{value}
```
