---
name: read-file
argument-hint: "{file = ./xyz}"
---
Use the `#tool:readFile` tool to read the files ${input:file}. If the file has a relative path, attempt to read it from the workspace. If the file has a `vscode-userdata:` protocol, read it using the full URL.

