# CRITICAL PLANNING RULES
When planning a Python project, you MUST stub all method and function declarations EXCLUSIVELY using `...`. 

Do not producce `*.pyi` files. Produce `*.py` files with stubs only.

![[rules/py.rules|rules py]]