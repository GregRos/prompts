# CRITICAL PLANNING RULES
When planning a TypeScript project, class, function, and variable declarations MUST be stubbed EXCLUSIVELY using `declare`.

Do not produce `*.d.ts` files. Produce `*.ts` files with declarations only.

![[rules/ts.rules/_|rules ts]]